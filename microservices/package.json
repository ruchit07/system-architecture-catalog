{
  "name": "microservices-saga-reference",
  "version": "0.1.0",
  "description": "Runnable saga reference — a multi-step cross-service workflow with compensation (the alternative to distributed transactions).",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "tsx --test src/saga.test.ts",
    "start": "tsx src/index.ts"
  },
  "license": "MIT",
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.0"
  }
}
