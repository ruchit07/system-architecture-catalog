{
  "name": "strangler-fig-reference",
  "version": "0.1.0",
  "description": "Runnable strangler fig: routing facade with per-route, metrics-based cutover and rollback.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "tsx --test src/facade.test.ts",
    "start": "tsx src/index.ts"
  },
  "license": "MIT",
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.0"
  }
}
