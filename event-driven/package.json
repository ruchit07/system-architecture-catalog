{
  "name": "event-driven-reference",
  "version": "0.1.0",
  "description": "Runnable event-driven reference — idempotent consumer + dead-letter queue over an at-least-once broker.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "tsx --test src/consumer.test.ts",
    "start": "tsx src/index.ts"
  },
  "license": "MIT",
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.0"
  }
}
